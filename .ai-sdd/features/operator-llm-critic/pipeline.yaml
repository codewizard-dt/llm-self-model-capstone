apiVersion: ai-sdd/v1
kind: Pipeline
metadata: { name: operator-llm-critic, version: 1 }
spec:
  semantics: enabler
  nodes:
    - { id: operator-packet-builder, kind: pipeline, pipeline: ../.., stack: operator }
    - { id: generator-prompt-and-fixtures, kind: pipeline, pipeline: ../.., stack: operator }
    - { id: generator-gap-revision, kind: pipeline, pipeline: ../.., stack: operator }
    - { id: critic-prompt-panel, kind: pipeline, pipeline: ../.., stack: operator }
    - { id: critic-review-aggregation, kind: pipeline, pipeline: ../.., stack: operator }
    - { id: planted-fault-critic-tests, kind: pipeline, pipeline: ../.., stack: operator }
  edges:
    - { from: operator-packet-builder, to: generator-prompt-and-fixtures }
    - { from: generator-prompt-and-fixtures, to: generator-gap-revision }
    - { from: generator-gap-revision, to: critic-prompt-panel }
    - { from: critic-prompt-panel, to: critic-review-aggregation }
    - { from: critic-review-aggregation, to: planted-fault-critic-tests }
