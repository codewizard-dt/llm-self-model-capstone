apiVersion: ai-sdd/v1
kind: Pipeline
metadata: { name: adapter-interfaces, version: 1 }
spec:
  semantics: enabler
  nodes:
    - { id: s1-protocols-and-exports, kind: pipeline, pipeline: ../.., stack: contracts }
  edges: []
