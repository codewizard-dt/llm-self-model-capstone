apiVersion: ai-sdd/v1
kind: Pipeline
metadata: { name: telemetry-fixture-baseline, version: 1 }
spec:
  semantics: enabler
  nodes:
    - { id: fixture-and-validation, kind: pipeline, pipeline: ../.., stack: contracts }
    - { id: downstream-loader-smoke, kind: pipeline, pipeline: ../.., stack: self_model_generator }
    - { id: docs-scope-alignment, kind: pipeline, pipeline: ../.., stack: coprocessor }
  edges:
    - { from: fixture-and-validation, to: downstream-loader-smoke }
    - { from: fixture-and-validation, to: docs-scope-alignment }
