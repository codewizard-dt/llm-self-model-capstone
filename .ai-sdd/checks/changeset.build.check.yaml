apiVersion: ai-sdd/v1
kind: Check
metadata: { name: changeset.build }
spec:
  # Explicit command (MASTER_REQUIREMENTS → Verification & Reviewer Runbook; ADR-15). Blocking.
  checkKind: deterministic
  command: "make sync"
  required: true
