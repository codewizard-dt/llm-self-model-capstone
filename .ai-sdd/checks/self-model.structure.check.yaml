apiVersion: ai-sdd/v1
kind: Check
metadata: { name: self-model.structure }
spec:
  # FROZEN-CONTRACT gate. Validates the committed canonical fixture against the frozen schema so the
  # contract stays valid for the whole project (your call on FLAG 2). Trusted executor; blocking.
  checkKind: deterministic
  command: "ai-sdd check .ai-sdd/schemas/self-model.schema.yaml .ai-sdd/schemas/examples/self-model.example.json"
  required: true
