apiVersion: ai-sdd/v1
kind: Check
metadata: { name: changeset.lint }
spec:
  # Explicit command (Verification & Reviewer Runbook; ADR-16): ruff check + ruff format --check.
  checkKind: deterministic
  command: "make lint"
  required: true
